import './PageBuilderColumn.style';
import PageBuilderWidget from '../PageBuilderWidget';

class PageBuilderColumn extends PageBuilderWidget {
    // TODO: override render
}

export default PageBuilderColumn;
